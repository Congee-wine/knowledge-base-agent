# Docker Worker override for a PostgreSQL SSH tunnel running on the host.
# Copy this file to .env.worker and replace every placeholder locally.
DATABASE_URL=postgresql://ai_app:replace-with-your-db-password@host.docker.internal:5432/ai_platform
