# Runtime dependencies for the document parsing worker in phase 3.2B.
# Embedding dependencies remain in requirements.txt and are added to this image
# only when the vectorization worker is implemented.
python-dotenv>=1.0,<2.0
psycopg[binary]>=3.2,<4.0
psycopg_pool>=3.2,<4.0
minio>=7.2,<8.0
redis>=5.0,<7.0
rq>=2.0,<3.0
PyMuPDF>=1.24,<2.0
python-docx>=1.1,<2.0
