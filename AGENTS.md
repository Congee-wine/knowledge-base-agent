# 项目开发约束

## 1. 项目定位

本项目是一个需要持续迭代和维护的正式工程，不是一次性演示项目。

“MVP”只代表当前功能范围有限，不代表可以降低以下方面的质量要求：

* 项目结构
* 模块边界
* 类型安全
* 错误处理
* 可测试性
* 可维护性
* 数据库设计
* 接口设计
* 安全性
* 后续扩展能力

不要使用“对于 MVP 项目已经足够”“先这样实现就行”等理由，来合理化明显不合理的目录结构、职责混乱、超大文件、重复代码、硬编码或临时方案。

在避免过度设计的同时，必须保持合理的工程结构。优先选择简单、清晰、可扩展的实现，而不是最快但难以维护的实现。

## 2. 开发前必须先分析

在新增功能或进行较大修改之前，先检查现有项目结构和相关代码，确认：

1. 功能应当属于哪个模块。
2. 当前项目是否已有相似实现可以复用。
3. 修改会影响哪些前端页面、后端接口、数据库表或智能体工作流。
4. 是否会造成重复逻辑、循环依赖或职责混乱。
5. 是否需要补充类型、测试、错误处理或文档。
6. 新代码应放在哪个已有目录中，是否真的需要创建新目录。

不得在不了解现有结构的情况下直接创建大量新文件或重新实现已有功能。

对于非简单修改，在开始编码前先简要说明：

* 对需求的理解
* 涉及的模块
* 计划修改的文件
* 采用该方案的原因
* 可能存在的风险

当当前任务属于需要独立功能设计文档的重大功能时，未完成需求确认、设计文档生成和需求一致性检查之前，不得直接开始完整功能编码；可以进行代码阅读、技术调研和现状分析，但不得将未确认的假设固化为正式实现。

## 3. 架构与目录结构

始终优先保证职责清晰和依赖方向合理。

### 前端职责

前端代码建议按照以下职责划分：

* `pages` 或 `routes`：页面级组合、路由入口
* `components`：可复用 UI 组件
* `features`：按照业务功能组织的模块
* `hooks`：可复用状态逻辑和副作用逻辑
* `services` 或 `api`：HTTP、WebSocket 和其他外部请求
* `stores`：跨组件共享状态
* `types`：共享 TypeScript 类型
* `utils`：无业务状态的纯工具函数
* `constants`：明确的共享常量

遵守以下规则：

* 不要在展示型组件中直接编写复杂接口请求。
* 不要把大量业务逻辑堆积在页面组件中。
* 不要把所有共享逻辑都放入一个通用 `utils` 文件。
* 页面负责组合，业务模块负责业务，组件负责展示。
* API 请求、错误转换和认证处理应尽量集中管理。
* React 组件应保持单一职责，复杂状态逻辑优先抽取到 Hook 或业务模块。
* 不要为了减少文件数量，把无关职责合并在同一个文件中。

### 后端职责

后端代码应按照职责进行分层：

* `routers`：路由声明、参数接收、依赖注入和响应返回
* `schemas`：请求与响应的数据结构
* `services`：业务规则和用例编排
* `repositories`：数据库访问和持久化逻辑
* `models`：ORM 数据模型
* `agents`：智能体定义、工具注册和执行逻辑
* `workflows`：LangGraph 或其他工作流编排
* `retrieval`：文档切分、向量检索、召回和重排
* `core`：配置、安全、日志和公共基础能力
* `integrations`：第三方模型、存储和外部服务适配

遵守以下规则：

* 路由层不得堆积复杂业务逻辑。
* Service 层不得直接依赖 HTTP 请求或响应对象。
* 数据库查询不得散落在路由、智能体节点和工具函数中。
* 智能体工作流与普通业务服务应保持清晰边界。
* Prompt、模型配置和业务代码不得全部混合在一个文件中。
* 不要让单个“万能服务”负责认证、检索、模型调用、数据库和响应格式化。
* 模块依赖应尽量保持单向，避免循环依赖。

## 4. 文件行数限制

所有人工编写的源代码文件，原则上不得超过 350 行。

具体要求：

* 推荐单个文件保持在 100～250 行。
* 文件接近 300 行时，应检查是否承担了过多职责。
* 文件超过 350 行时，必须在完成任务前进行合理拆分。
* 不得通过删除空行、压缩代码或把多条语句写在同一行的方式规避限制。
* 拆分必须基于职责、领域或功能边界，不能为了行数而机械拆分。
* 拆分后不得造成循环依赖、过多转发文件或难以理解的调用链。

以下文件可以例外：

* 自动生成的代码
* 数据库迁移文件
* 依赖锁文件
* 大型静态数据文件
* 由框架或工具自动维护的文件

如果由于特殊原因必须让人工维护的文件超过 350 行，需要明确说明：

* 为什么无法合理拆分
* 文件当前包含哪些职责
* 后续可以如何重构

## 5. 函数和组件约束

* 函数应只完成一个明确任务。
* 普通函数尽量控制在 50 行以内。
* 复杂函数应拆分为有明确名称的子函数。
* 避免超过三层的深度嵌套。
* 避免包含大量布尔参数的函数。
* 避免过长的参数列表，必要时使用明确的数据对象。
* React 页面或组件过于复杂时，应拆分展示组件、业务 Hook 和数据访问逻辑。
* 不要创建只有一行转发逻辑、没有实际抽象价值的文件或函数。

## 6. 代码质量

编写代码时必须遵守以下原则：

* 优先复用现有能力，不重复实现相同逻辑。
* 使用清晰、准确的命名，不使用含义模糊的缩写。
* 避免魔法数字、魔法字符串和散落的配置值。
* 不使用临时占位实现冒充完整功能。
* 不遗留无说明的 `TODO`、`FIXME` 或被注释掉的旧代码。
* 注释应解释“为什么这样做”，而不是重复描述代码本身。
* 不捕获异常后静默忽略。
* 错误信息应对开发者可定位，对用户可理解。
* 外部输入必须进行校验。
* 对可能为空、失败或超时的操作进行明确处理。
* TypeScript 中避免随意使用 `any`、非空断言和不安全的类型转换。
* Python 代码应尽量提供完整类型注解。
* 不得在代码中硬编码密钥、Token、密码或生产环境地址。

## 7. 数据库与接口设计

* 数据表、字段和关系必须反映真实业务含义。
* 不要为了快速实现，把结构化数据全部存入一个 JSON 字段。
* 明确主键、唯一约束、外键、索引和删除策略。
* 修改数据库结构时，应考虑已有数据和迁移方案。
* 避免在循环中进行大量数据库查询。
* 接口应保持稳定、统一和可预测。
* 请求和响应结构必须有明确类型。
* 错误响应应采用统一格式。
* 分页、筛选、排序和权限校验应由后端明确实现。
* 不要直接把数据库模型作为所有接口的响应结构。

## 8. 知识库与智能体约束

涉及 RAG、LangChain、LangGraph 或智能体功能时，需要明确区分：

* 文档加载
* 文档解析
* 文本切分
* Embedding
* 向量存储
* 检索
* 重排
* 上下文构造
* Prompt 管理
* 模型调用
* Tool Calling
* 工作流状态
* 最终回答生成

遵守以下规则：

* 不要把完整 RAG 流程写在单个路由或单个服务文件中。
* Prompt 应集中管理，并具有明确名称和用途。
* 模型供应商相关代码应通过适配层隔离。
* 工作流节点只处理自身职责，不承担整个业务流程。
* 工作流状态应有明确的数据结构和类型。
* 工具调用必须校验参数并处理失败情况。
* 检索结果、模型回答和工具结果应能被区分和追踪。
* 不要依赖无法解释的隐式全局状态。
* 对超时、空检索结果、模型失败和格式异常提供降级处理。
* 调整检索策略时，应说明召回率、准确率、延迟和成本之间的取舍。

## 9. 依赖管理

* 优先使用项目已有依赖和标准库。
* 不要为了一个简单功能引入大型依赖。
* 新增生产依赖前，先说明：

  * 现有方案为什么不能满足需求
  * 新依赖解决什么问题
  * 是否存在更轻量的替代方案
  * 对构建体积、安全性和维护成本的影响
* 不得未经说明升级主要框架或进行大版本升级。
* 前端依赖统一使用项目当前指定的包管理器。
* 不要混用 npm、pnpm 和 yarn。

## 10. 修改范围控制

* 修改应聚焦当前任务。
* 不要顺手重构大量无关代码。
* 不要随意更改已有公开接口、数据结构或配置格式。
* 必须进行破坏性修改时，要明确说明影响范围和迁移方法。
* 优先延续项目已有命名、目录和代码风格。
* 如果现有结构明显不合理，可以提出重构方案，但不要在没有说明的情况下大范围改写。
* 不要为了“代码更少”牺牲可读性和模块边界。

## 11. 测试与检查

修改完成后，根据项目现有能力执行适用的检查：

* 格式化检查
* Lint
* TypeScript 类型检查
* Python 类型检查
* 单元测试
* 接口测试
* 构建检查

新增或修改以下逻辑时，应优先补充测试：

* 登录认证
* Token 刷新
* 权限判断
* 数据转换
* 数据库操作
* 检索逻辑
* 工作流状态转换
* 错误处理
* 核心业务规则

如果无法执行检查，必须明确说明：

* 哪些检查没有执行
* 为什么无法执行
* 可能存在什么风险

不得在未执行测试时声称代码已经完全验证。

## 12. 方案判断原则

进行技术方案选择时，按照以下优先级判断：

1. 正确性
2. 安全性
3. 可维护性
4. 与现有架构的一致性
5. 可测试性
6. 实现复杂度
7. 开发速度

MVP 可以减少功能范围，但不能成为牺牲正确性、安全性和基本工程质量的理由。

避免以下表达和处理方式：

* “对于 MVP 来说已经足够。”
* “先全部写在一个文件里，后面再拆。”
* “暂时硬编码，之后再处理。”
* “现在不需要考虑错误情况。”
* “项目不大，所以不需要分层。”
* “先复制一份相似代码，之后再统一。”

应改为说明具体取舍：

* 当前方案解决什么问题。
* 为什么适合当前项目规模。
* 保留了哪些扩展边界。
* 暂时没有实现哪些功能。
* 未实现部分是否会形成技术债务。
* 后续需求增长时应从哪里扩展。

## 13. 完成任务后的输出

完成修改后，使用简体中文简要说明：

1. 修改了哪些文件。
2. 每个文件承担什么职责。
3. 为什么采用当前结构。
4. 是否新增或修改依赖。
5. 是否涉及数据库或接口变化。
6. 执行了哪些检查或测试。
7. 是否存在尚未解决的风险。

不要只回复“已完成”或“这个实现对于 MVP 已经足够”。

## 14. 项目文档与开发过程同步

本项目需要持续维护项目文档。文档更新属于开发任务的一部分，不是可选步骤。

当代码、项目结构、接口、数据库、功能状态、技术方案或开发计划发生变化时，必须同步检查并更新 `docs/` 目录中的相关文档。

不要只在对话中说明项目变化，而不更新项目内的文档。

### 14.1 文档目录

项目文档统一存放在：

```text
docs/
├── README.md
├── PROJECT_STATUS.md
├── DEVELOPMENT_LOG.md
├── QUESTIONS.md
├── DECISIONS.md
├── ARCHITECTURE.md
└── features/
    └── <feature-name>/
```

各文件职责如下：

* `docs/README.md`：文档索引，说明各文档的用途。
* `docs/PROJECT_STATUS.md`：当前项目整体进度和下一步计划。
* `docs/DEVELOPMENT_LOG.md`：按照时间记录实际开发过程。
* `docs/QUESTIONS.md`：记录开发过程中提出的重要问题及最终结论。
* `docs/DECISIONS.md`：记录重要技术决策、选择理由和影响。
* `docs/ARCHITECTURE.md`：记录当前项目结构、模块职责和主要数据流。
* `docs/features/<feature-name>/`：保存已经确认的重大功能需求及其前端、后端、接口、数据和验收设计。

如果这些文件不存在，可以在首次需要时创建。

不要为每次小型开发任务创建新的零散文档。项目总体状态、开发日志、问题、决策和架构仍然优先维护以上固定文件。对于已经由用户正式确认、涉及完整业务流程或前后端协作的重大功能，可以按照 14.11 的规定，在 `docs/features/<feature-name>/` 下创建独立功能设计目录。不得在 `docs/` 根目录随意创建命名不统一、职责重复的需求文档。

### 14.2 开始任务前

开始处理项目任务前，先读取：

1. 根目录的 `AGENTS.md`。
2. `docs/PROJECT_STATUS.md`。
3. 与当前任务相关的架构、决策和开发记录。
4. 如果当前功能已有设计目录，读取 `docs/features/<feature-name>/` 中的需求、前端、后端、接口、数据和验收文档。
5. 当前任务涉及的源代码和测试。

开始编码前，需要确认：

* 当前项目处于什么阶段。
* 当前正在开发什么功能。
* 是否存在相关的历史决策。
* 当前任务是否与已有计划冲突。
* 是否存在未完成事项、阻塞项或已知风险。
* 是否已经有相似功能，避免重复实现。

不得只根据当前对话内容判断项目状态，而忽略项目中已有的文档和代码。

如果项目文档与实际代码不一致，以实际代码和可验证的运行结果为依据，并在本次任务中修正文档。

### 14.3 项目进度同步

`docs/PROJECT_STATUS.md` 是项目当前状态的唯一主要入口。

该文件至少维护以下内容：

```md
# 项目当前状态

## 当前阶段

说明项目目前处于需求设计、基础架构、功能开发、联调、测试或部署中的哪个阶段。

## 当前目标

说明当前阶段最重要的目标。

## 已完成功能

- 已完成并经过验证的功能
- 对应的模块
- 验证状态

## 正在进行

- 当前正在开发的功能
- 已完成部分
- 尚未完成部分

## 下一步计划

1. 下一项具体任务
2. 后续任务
3. 需要准备的内容

## 阻塞问题

- 当前阻塞项
- 阻塞原因
- 解除阻塞所需条件

## 已知风险与技术债务

- 风险或技术债务
- 可能造成的影响
- 建议处理时间

## 最近更新时间

YYYY-MM-DD
```

发生以下情况时，必须更新 `PROJECT_STATUS.md`：

* 完成一个功能或阶段性目标。
* 开始一个新的主要功能。
* 项目计划发生变化。
* 新增或解除阻塞问题。
* 发现重要风险或技术债务。
* 修改主要项目结构。
* 修改核心接口、数据库或智能体工作流。
* 原有进度记录与实际代码不一致。

只记录真实且经过确认的状态。

不得因为生成了代码就直接将功能标记为“已完成”。只有完成必要检查，并确认功能达到当前验收条件后，才可以标记为已完成。

状态建议使用：

* `未开始`
* `进行中`
* `已完成`
* `已阻塞`
* `待验证`

不要使用含义不明确的状态，例如：

* `差不多完成`
* `基本可用`
* `MVP 已经够用`
* `后面再优化`

### 14.4 开发过程记录

每次完成有实际意义的代码修改后，在 `docs/DEVELOPMENT_LOG.md` 中追加开发记录。

使用以下格式：

```md
## YYYY-MM-DD：任务名称

### 任务目标

说明本次任务准备解决什么问题。

### 实现内容

- 实现了哪些功能
- 修改了哪些模块
- 增加了哪些关键逻辑

### 主要文件

- `path/to/file`：说明该文件的修改内容和职责
- `path/to/file`：说明该文件的修改内容和职责

### 技术方案

说明采用了什么方案，以及选择该方案的主要原因。

### 接口或数据变化

- 是否新增或修改接口
- 是否修改请求和响应结构
- 是否修改数据库表或字段
- 是否需要执行迁移

没有变化时明确写“无”。

### 验证情况

- 执行了哪些测试
- 执行了哪些检查
- 哪些检查通过
- 哪些检查未执行及原因

### 遗留问题

- 尚未完成的内容
- 已知风险
- 后续建议
```

开发日志按时间顺序维护。

不要修改或删除过去的有效记录来掩盖问题。如果之前的记录已经失效，应追加更正说明，而不是直接让历史记录消失。

以下情况可以不单独记录：

* 只修改错别字。
* 只调整无业务影响的格式。
* 没有改变代码行为的小范围注释修正。
* 没有实际修改项目文件的咨询性对话。

### 14.5 用户问题记录

对于用户提出的、与当前项目开发直接相关且具有长期参考价值的问题，应记录到：

```text
docs/QUESTIONS.md
```

需要记录的问题包括：

* 项目结构应该如何设计。
* 某个功能应该放在哪一层。
* 为什么选择某项技术。
* 接口、数据库或认证应该如何设计。
* 某个错误的原因和解决方式。
* 项目需求或业务规则的澄清。
* 对后续开发有影响的约束。
* 用户最终确认的实现方向。

以下内容通常不需要记录：

* 单纯询问某行代码的语法。
* 与项目无关的通用知识问题。
* 重复提出且结论没有变化的问题。
* 临时交流和无长期价值的内容。
* 尚未形成结论的随意猜测。

问题记录使用以下格式：

```md
## Q-YYYYMMDD-序号：问题标题

- **日期**：YYYY-MM-DD
- **状态**：待确认 / 已确认 / 已解决
- **涉及模块**：模块名称
- **用户问题**：准确概括用户提出的问题
- **问题背景**：说明为什么会提出这个问题
- **最终结论**：记录经过确认的答案或决定
- **实现影响**：说明会影响哪些模块、接口或文件
- **后续事项**：仍需处理的内容
```

记录用户问题时，需要进行有意义的概括，不要机械复制整段对话。

重点记录：

* 问题本身；
* 形成的结论；
* 对项目产生的影响；
* 是否还有后续工作。

同一问题被多次讨论时，应优先更新已有记录，不要重复创建多个内容相同的问题条目。

如果问题尚未形成可靠结论，应标记为“待确认”，不得把推测写成最终决定。

### 14.6 技术决策记录

发生重要技术选择时，应更新：

```text
docs/DECISIONS.md
```

需要记录的决策包括：

* 选择某个框架、数据库或核心依赖。
* 调整前后端目录结构。
* 修改认证方式。
* 修改接口规范。
* 修改数据库关系。
* 修改 RAG 检索流程。
* 修改智能体工作流。
* 引入新的第三方服务。
* 进行可能影响后续扩展的架构选择。

使用以下格式：

```md
## ADR-序号：决策标题

- **日期**：YYYY-MM-DD
- **状态**：提议 / 已采用 / 已废弃 / 已替代

### 背景

说明需要解决的问题和约束。

### 可选方案

1. 方案一
2. 方案二
3. 方案三

### 最终决定

说明采用哪个方案。

### 选择理由

说明为什么采用该方案，而不是其他方案。

### 影响与代价

说明方案带来的优点、限制、风险和维护成本。

### 后续条件

说明在什么情况下需要重新评估该决策。
```

不要把普通代码实现细节全部写成技术决策。只有对项目结构、长期维护或后续扩展有明显影响的选择，才需要进入决策记录。

### 14.7 架构文档同步

`docs/ARCHITECTURE.md` 应反映项目当前真实结构。

当发生以下变化时，应同步更新：

* 新增主要业务模块。
* 调整目录职责。
* 修改模块依赖方向。
* 增加第三方系统。
* 修改数据库、缓存、向量库或文件存储方案。
* 修改请求链路。
* 修改认证流程。
* 修改 RAG 或智能体执行流程。
* 修改前后端通信方式。

架构文档至少应包含：

* 项目总体说明。
* 前端模块划分。
* 后端模块划分。
* 数据库和存储说明。
* 认证流程。
* 核心接口调用流程。
* 知识库数据处理流程。
* 智能体工作流。
* 外部服务依赖。
* 关键模块之间的依赖关系。

架构文档应描述当前已经存在的实现。

计划中的架构必须明确标记为“规划中”，不能与已经实现的结构混在一起。

### 14.8 每次任务结束前的文档检查

完成任务前，必须执行一次文档同步检查：

1. 本次修改是否改变了功能状态。
2. 是否需要更新 `PROJECT_STATUS.md`。
3. 是否需要追加 `DEVELOPMENT_LOG.md`。
4. 用户是否提出了值得长期记录的问题。
5. 是否形成了新的技术决策。
6. 是否改变了项目架构或模块职责。
7. 文档中的路径、接口和状态是否仍然准确。
8. 文档是否包含未经验证的完成状态。

只更新与本次任务直接相关的文档。

不要为了满足“更新文档”而机械修改所有文档，也不要重写与本次任务无关的历史内容。

文档修改应与代码修改处于同一次任务中，不要把文档同步推迟到不确定的未来。

### 14.9 信息安全要求

项目文档中不得记录：

* 密码
* Access Token
* Refresh Token
* API Key
* 数据库完整连接字符串
* Cookie
* 私钥
* 用户隐私数据
* 生产环境敏感配置
* `.env` 文件中的真实值

需要说明配置时，只记录环境变量名称和用途，例如：

```text
DATABASE_URL：数据库连接地址
JWT_SECRET：JWT 签名密钥
OPENAI_API_KEY：模型服务访问密钥
```

不得记录这些变量的真实值。

### 14.10 文档真实性

所有开发记录必须基于实际修改和验证结果。

遵守以下要求：

* 没有执行测试时，不得写“测试已通过”。
* 没有完成的功能，不得记录为“已完成”。
* 仅创建接口骨架时，应标记为“进行中”。
* 仅完成前端页面时，不得声称整个业务功能已经完成。
* 发现代码与文档不一致时，应明确更正文档。
* 无法确认的信息应标记为“待确认”。
* 推测和规划不得写成已经实现的事实。


### 14.11 需求确认与功能设计文档

对于涉及完整业务流程、前后端协作、接口设计、数据库变化、权限控制、知识库流程或智能体工作流的功能，必须在需求正式确认后创建独立的功能设计文档。

功能设计文档是后续编码、测试、联调、验收和需求变更判断的主要依据。设计内容必须以用户最后一次明确确认的需求为准，不得擅自增加、删除、替换或弱化功能需求。

#### 14.11.1 适用范围

以下情况需要创建独立功能设计目录：

* 新增一个完整业务功能。
* 新增或明显调整前后端交互流程。
* 新增或修改数据库表、字段、关系、索引或数据生命周期。
* 新增认证、权限、角色或资源访问规则。
* 新增知识库上传、解析、切分、索引、检索、重排或问答流程。
* 新增或修改智能体、工具调用、工作流节点或 LangGraph 状态流转。
* 功能涉及多个页面、多个接口或多个后端模块。
* 功能存在较多业务规则、异常分支、状态转换或并发一致性要求。
* 用户明确要求生成前后端设计文档。

以下情况通常不需要创建独立功能目录：

* 修改文案、样式或静态资源。
* 修复局部且不改变业务规则的缺陷。
* 小范围代码重构。
* 只修改一个内部工具函数。
* 尚未形成明确结论的需求讨论。

不得为每个小任务机械创建新目录。

#### 14.11.2 需求确认条件

只有满足以下条件，才可以将需求标记为“已确认”，并创建或更新正式功能设计文档：

1. 功能背景和目标已经明确。
2. 使用者、角色和权限范围已经明确。
3. 功能入口和主要使用流程已经明确。
4. 前端页面、交互和展示结果已经明确。
5. 后端需要执行的业务规则已经明确。
6. 需要保存的数据、数据归属和数据生命周期已经明确。
7. 关键异常情况和边界条件已经明确。
8. 与现有功能的关系和影响范围已经明确。
9. 本次包含与不包含的范围已经明确。
10. 验收条件已经明确。
11. 用户已经明确表示确认当前需求或当前方案。

用户明确确认的表达包括但不限于：

* “需求确定了”
* “就按照这个方案”
* “没有问题，可以开始设计”
* “按上面的需求实现”
* “确认”
* 其他含义明确的确认表达

不得因为需求讨论暂时没有新问题，就自行判断需求已经确认。

不得把智能体自己的推测、建议、常见做法或默认选择当作用户已经确认的需求。

如果仍存在会影响页面交互、接口契约、数据结构、权限规则、业务结果或验收标准的问题，应继续标记为“待确认”，不得开始完整功能编码。

#### 14.11.3 需求确认前的处理

在需求讨论过程中，应持续整理：

* 已确认需求。
* 待确认问题。
* 用户明确排除的功能。
* 用户提出的限制条件。
* 当前候选方案。
* 不同方案之间的差异和影响。
* 可能影响前端、后端、数据库、智能体、测试和部署的事项。

需求尚未确认时，可以在对话中提供阶段性需求总结，但必须明确标记为“草案”或“待确认”，不得写成最终设计结论。

具有长期参考价值的待确认事项，应记录到：

```text
docs/QUESTIONS.md
```

状态必须标记为：

```text
待确认
```

如果用户明确要求只进行需求讨论，不修改项目文件，则只在对话中整理需求，不创建正式功能设计目录。

#### 14.11.4 功能设计目录

需求正式确认后，在以下目录创建或更新功能设计文件夹：

```text
docs/features/<feature-name>/
```

`<feature-name>` 使用简短、稳定、能够表达业务含义的英文小写名称，单词之间使用连字符，例如：

```text
docs/features/user-authentication/
docs/features/knowledge-base-management/
docs/features/document-upload/
docs/features/agent-chat/
docs/features/conversation-history/
```

不得使用含义模糊或容易失效的名称，例如：

```text
docs/features/new-feature/
docs/features/temp/
docs/features/test/
docs/features/需求1/
docs/features/最终版/
```

如果项目中已经存在同一功能的设计目录，应更新原有目录，不得重复创建以下形式的目录：

```text
agent-chat/
agent-chat-v2/
agent-chat-final/
agent-chat-new/
```

除非确实存在两个需要长期并行维护的独立版本，并且用户明确确认并行维护，否则应在原设计目录中记录变更。

#### 14.11.5 功能设计目录结构

每个重大功能的设计目录默认包含：

```text
docs/features/<feature-name>/
├── README.md
├── REQUIREMENTS.md
├── FRONTEND_DESIGN.md
├── BACKEND_DESIGN.md
├── API_DESIGN.md
├── DATA_DESIGN.md
├── ACCEPTANCE_CRITERIA.md
└── CHANGELOG.md
```

应根据功能实际情况创建文件。

如果某项确实不涉及，例如功能不修改数据库，可以不创建 `DATA_DESIGN.md`，但必须在 `README.md` 中明确说明：

```text
本功能不涉及数据库结构、缓存结构或其他持久化方案变化。
```

不得为了凑齐目录结构而填写无意义内容。

#### 14.11.6 README.md

`README.md` 是该功能设计文档的统一入口，至少包含：

```md
# 功能名称

## 文档状态

草稿 / 待确认 / 已确认 / 实现中 / 待验证 / 已实现 / 已废弃

## 功能概述

说明该功能解决什么问题，以及主要使用者是谁。

## 需求确认信息

- 确认日期：
- 确认依据：
- 当前版本：
- 需求来源：
- 最后确认人：用户

## 文档列表

- `REQUIREMENTS.md`：最终需求和业务规则
- `FRONTEND_DESIGN.md`：前端页面、组件、状态和交互设计
- `BACKEND_DESIGN.md`：后端模块、业务流程和异常处理设计
- `API_DESIGN.md`：前后端接口契约
- `DATA_DESIGN.md`：数据库、缓存和存储设计
- `ACCEPTANCE_CRITERIA.md`：验收标准
- `CHANGELOG.md`：需求和设计变更记录

## 实现状态

- 前端：
- 后端：
- 数据库：
- 接口联调：
- 测试：

## 相关全局文档

- `docs/PROJECT_STATUS.md`
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md`
- `docs/QUESTIONS.md`
```

只有用户明确确认需求后，文档状态才可以标记为“已确认”。

只有代码实现完成并达到当前验收条件，经过必要检查后，才可以标记为“已实现”。

#### 14.11.7 REQUIREMENTS.md

`REQUIREMENTS.md` 必须完整记录用户最后确认的功能需求，是功能实现的首要依据。

至少包含：

```md
# 功能需求

## 1. 功能背景

说明用户为什么需要该功能，以及当前需要解决的问题。

## 2. 功能目标

说明功能完成后应达到什么结果。

## 3. 用户角色

说明哪些角色可以使用该功能，以及不同角色之间的权限差异。

## 4. 功能范围

### 4.1 本次包含

- 已确认需要实现的内容

### 4.2 本次不包含

- 用户明确排除的内容
- 计划以后实现的内容
- 为避免范围扩张而暂不实现的内容

## 5. 核心业务流程

按照实际执行顺序描述完整流程。

## 6. 详细功能需求

### FR-001：需求名称

- 功能描述：
- 使用角色：
- 前置条件：
- 触发方式：
- 正常流程：
- 异常流程：
- 输入：
- 输出：
- 权限要求：
- 业务规则：
- 数据变化：
- 验收条件：

## 7. 状态与状态转换

说明业务对象有哪些状态、初始状态、允许的转换、禁止的转换和转换触发条件。

## 8. 边界情况

- 空数据
- 重复操作
- 非法参数
- 并发操作
- 网络失败
- 权限不足
- 数据不存在
- 数据状态冲突
- 服务超时
- 第三方服务失败
- 用户中途取消
- 登录状态失效

## 9. 非功能需求

- 性能
- 安全
- 可用性
- 可维护性
- 日志
- 可观测性
- 兼容性
- 可测试性

## 10. 已确认约束

记录用户明确提出的技术、业务、交互、时间和范围限制。

## 11. 待确认事项

已确认版本中原则上不应存在会阻塞实现的待确认事项。

如果存在不阻塞实现的问题，必须明确说明其影响范围、暂定处理方式和后续确认节点。
```

必须使用准确、可验证的表达。

避免使用：

* “支持常规操作”
* “提供友好的提示”
* “适当进行校验”
* “根据情况处理”
* “尽量保证性能”
* “等等”
* “类似现有功能”

应明确说明具体行为、触发条件、业务结果和失败结果。

#### 14.11.8 FRONTEND_DESIGN.md

前端设计必须基于已经确认的需求和项目现有结构，不得擅自增加页面、按钮、字段或交互。

至少包含：

````md
# 前端设计

## 1. 设计目标和范围

说明本次前端需要实现什么，以及明确不负责什么。

## 2. 涉及页面与路由

| 页面 | 路由 | 使用角色 | 页面职责 | 访问条件 |
|---|---|---|---|---|

## 3. 页面结构

说明每个页面包含哪些区域、区域职责、显示条件和数据来源。

## 4. 组件设计

| 组件 | 类型 | 职责 | 主要输入 | 主要输出 | 状态来源 |
|---|---|---|---|---|---|

明确区分：

- 页面组件
- 业务组件
- 展示组件
- 表单组件
- 弹窗组件
- 公共组件

## 5. 状态设计

说明：

- 页面本地状态
- 表单状态
- 服务端状态
- 跨组件共享状态
- 跨页面共享状态
- URL 查询参数
- 持久化状态
- 状态初始化和清理时机

不得把所有状态默认放入全局 Store。

## 6. 数据请求

说明：

- 页面初始化请求
- 用户操作触发的请求
- 请求参数来源
- 请求前校验
- 请求成功后的处理
- 请求失败后的处理
- 重试策略
- 是否需要取消请求
- 是否需要防抖或节流
- 是否需要缓存和重新验证
- 登录失效如何统一处理

## 7. 交互流程

按照用户操作顺序描述：

1. 用户执行什么操作。
2. 前端进行什么校验。
3. 页面进入什么临时状态。
4. 调用什么接口。
5. 成功后展示什么并更新哪些状态。
6. 失败后展示什么并保留或回滚哪些状态。

## 8. 页面状态

每个主要页面或区域必须考虑：

- 初始状态
- 加载状态
- 空状态
- 成功状态
- 局部失败状态
- 整体失败状态
- 无权限状态
- 登录失效状态
- 数据不存在状态
- 提交中状态
- 重复提交状态

## 9. 表单与校验

| 字段 | 类型 | 是否必填 | 默认值 | 校验规则 | 错误提示 |
|---|---|---|---|---|---|

## 10. 权限与路由保护

说明：

- 哪些页面需要登录。
- 哪些角色能够访问。
- Token 过期如何处理。
- 无权限时如何跳转或提示。
- 是否允许用户看到部分数据。
- 前端权限控制与后端权限校验的边界。

## 11. 类型设计

列出主要 TypeScript 类型及其职责。

前端类型必须与接口契约一致，但不得直接依赖后端 ORM 模型。

## 12. 目录与文件规划

```text
src/
├── ...
```

列出计划新增或修改的主要文件，并说明每个文件的职责。

规划必须结合项目现有目录，不得脱离现有项目结构凭空设计。

## 13. 前端测试设计

说明需要覆盖的：

- 组件测试
- Hook 测试
- 表单校验测试
- 权限测试
- 请求失败测试
- 状态恢复测试
- 核心流程测试
- 必要的端到端测试
````

#### 14.11.9 BACKEND_DESIGN.md

后端设计必须覆盖完整业务规则、依赖方向和失败路径。

至少包含：

````md
# 后端设计

## 1. 设计目标和范围

说明后端需要负责什么，以及明确不负责什么。

## 2. 涉及模块

| 模块 | 职责 | 是否新增 | 影响范围 | 上游依赖 | 下游依赖 |
|---|---|---|---|---|---|

## 3. 分层设计

说明功能分别由哪些层负责：

- Router
- Schema
- Service
- Repository
- Model
- Agent
- Workflow
- Retrieval
- Integration

不得只列文件名称，必须说明职责和依赖方向。

## 4. 核心业务流程

按照后端实际执行顺序描述：

1. 接收请求。
2. 身份认证。
3. 参数校验。
4. 权限判断。
5. 业务规则检查。
6. 数据读取。
7. 数据写入或状态变更。
8. 第三方服务、模型或工具调用。
9. 事务提交。
10. 响应转换与返回。
11. 异常处理和日志记录。

## 5. 业务规则

使用编号记录，例如：

```text
BR-001：
BR-002：
```

每条业务规则必须能够追溯到需求或验收标准。

## 6. 事务与一致性

说明：

- 哪些操作需要事务。
- 事务边界在哪里。
- 部分失败如何回滚。
- 重复请求如何处理。
- 是否需要幂等性。
- 并发修改如何处理。
- 数据库与缓存如何保持一致。
- 异步任务失败如何补偿。

## 7. 权限与安全

说明：

- 身份认证方式。
- 资源所有权校验。
- 角色权限。
- 输入校验。
- 敏感数据处理。
- 日志脱敏。
- 请求频率限制。
- 文件安全校验。
- 第三方回调验签。

## 8. 异常处理

| 场景 | 异常类型 | HTTP 状态码 | 错误码 | 用户提示 | 是否重试 | 是否记录日志 |
|---|---|---:|---|---|---|---|

## 9. 日志与可观测性

说明：

- 需要记录的关键操作。
- 请求链路标识。
- 错误日志。
- 模型调用日志。
- 工具调用日志。
- 工作流节点日志。
- 性能指标。
- 不得记录的敏感信息。

## 10. 目录与文件规划

列出计划新增或修改的后端文件，并说明职责和依赖关系。

## 11. 后端测试设计

说明需要覆盖的：

- Service 单元测试
- Repository 测试
- 接口测试
- 权限测试
- 事务回滚测试
- 幂等性测试
- 异常分支测试
- 工作流状态测试
- 第三方服务失败测试
````

#### 14.11.10 API_DESIGN.md

前后端开发必须以同一份接口契约为依据。

接口设计至少包含：

````md
# 接口设计

## 1. 通用约定

说明：

- API 基础路径
- 认证方式
- 时间和时区格式
- 分页格式
- 排序和筛选规则
- 成功响应格式
- 错误响应格式
- 错误码命名规则
- 幂等性规则

## 2. 接口列表

| 编号 | 方法 | 路径 | 用途 | 权限 | 对应需求 |
|---|---|---|---|---|---|

## API-001：接口名称

### 基本信息

- 方法：
- 路径：
- 认证要求：
- 使用角色：
- 幂等性要求：
- 对应需求：
- 调用时机：

### Path 参数

| 参数 | 类型 | 必填 | 校验规则 | 说明 |
|---|---|---|---|---|

### Query 参数

| 参数 | 类型 | 必填 | 默认值 | 校验规则 | 说明 |
|---|---|---|---|---|---|

### 请求体

```json
{}
```

### 请求字段

| 字段 | 类型 | 必填 | 是否可空 | 默认值 | 校验规则 | 说明 |
|---|---|---|---|---|---|---|

### 成功响应

```json
{}
```

### 响应字段

| 字段 | 类型 | 是否可空 | 说明 |
|---|---|---|---|

### 错误响应

| HTTP 状态码 | 错误码 | 触发条件 | 用户提示 |
|---:|---|---|---|

### 业务规则

说明该接口必须遵守的业务规则。

### 前端处理

说明前端成功、失败和登录失效时分别如何处理。
````

接口文档必须明确字段是否可为空、时间格式、枚举范围、分页规则和错误格式。

不得使用“返回相关数据”等模糊描述。

#### 14.11.11 DATA_DESIGN.md

涉及数据库、Redis、向量库、对象存储或文件存储时，必须创建数据设计文档。

至少包含：

```md
# 数据设计

## 1. 存储范围

说明分别使用以下存储保存什么数据：

- 关系型数据库
- Redis
- 向量数据库
- 对象存储
- 本地文件系统

## 2. 实体关系

说明主要实体、实体职责、实体关系和数据归属。

## 3. 数据表设计

### DB-001：表名

表用途：

| 字段 | 类型 | 是否为空 | 默认值 | 约束 | 业务含义 |
|---|---|---|---|---|---|

### 索引

| 索引 | 字段 | 类型 | 用途 | 预期查询场景 |
|---|---|---|---|---|

### 外键和删除策略

说明：

- 外键关系
- 级联删除
- 限制删除
- 软删除
- 数据保留规则
- 审计字段

## 4. 缓存设计

说明：

- Key 格式
- Value 结构
- TTL
- 写入时机
- 读取时机
- 失效时机
- 缓存穿透处理
- 缓存击穿处理
- 缓存一致性策略

## 5. 向量与文档存储设计

说明：

- 原始文档存储位置
- 文档版本
- 分块结构
- Embedding 模型
- 向量集合或命名空间
- 元数据字段
- 删除和重建策略

## 6. 数据迁移

说明：

- 是否需要迁移。
- 如何兼容已有数据。
- 是否需要回填。
- 是否可以回滚。
- 发布顺序。
- 失败恢复方式。

## 7. 数据安全

说明：

- 敏感字段。
- 是否需要加密或脱敏。
- 数据访问权限。
- 数据保留期限。
- 日志中不得记录的字段。
```

不得为了方便而把明确的结构化业务数据全部放入 JSON 字段。

#### 14.11.12 ACCEPTANCE_CRITERIA.md

验收标准必须能够直接判断功能是否符合用户最后确认的需求。

使用以下格式：

```md
# 验收标准

## AC-001：验收项名称

- **对应需求**：FR-001
- **对应接口**：API-001
- **对应业务规则**：BR-001
- **前置条件**：
- **操作步骤**：
  1.
  2.
  3.
- **预期结果**：
- **异常情况**：
- **验证方式**：人工验证 / 单元测试 / 接口测试 / 端到端测试
- **当前状态**：未验证 / 已通过 / 未通过
- **验证证据**：
```

验收标准应根据功能实际情况覆盖：

* 正常流程。
* 权限限制。
* 参数错误。
* 空数据。
* 重复操作。
* 网络或服务失败。
* 登录失效。
* 用户取消操作。
* 数据不存在。
* 数据状态冲突。
* 必要的并发场景。
* 第三方服务异常。

不得只写“功能可以正常使用”。

#### 14.11.13 CHANGELOG.md

需求确认后发生的任何需求或设计变化，都必须追加到 `CHANGELOG.md`：

```md
## YYYY-MM-DD：变更标题

- **变更类型**：需求新增 / 需求修改 / 需求删除 / 设计调整
- **变更状态**：提议 / 已确认 / 已实施 / 已撤销
- **变更原因**：
- **变更前**：
- **变更后**：
- **用户是否确认**：是 / 否
- **影响的需求编号**：
- **影响的前端模块**：
- **影响的后端模块**：
- **影响的接口**：
- **影响的数据结构**：
- **是否需要修改测试**：
- **是否需要迁移已有数据**：
```

不得直接覆盖旧需求而不保留变更记录。

用户尚未确认的修改只能记录为“提议”，不得直接作为实现依据。

#### 14.11.14 需求到设计的追踪关系

必须保证需求、设计、实现和验收之间可追踪。

统一使用编号：

```text
FR-001：功能需求
BR-001：业务规则
API-001：接口
DB-001：数据实体或数据表
AC-001：验收标准
```

设计文档中应注明对应关系，例如：

```text
FR-001
├── FRONTEND_DESIGN：用户创建知识库表单
├── API-001：创建知识库接口
├── BR-001：名称唯一性规则
├── DB-001：knowledge_bases 表
└── AC-001：成功创建知识库
```

不得出现以下情况：

* 已设计的页面没有对应需求。
* 已设计的接口没有调用场景。
* 数据表字段没有业务用途。
* 已确认需求没有对应实现方案。
* 验收标准无法追溯到具体需求。
* 前端和后端对字段、状态或错误处理的理解不一致。
* 代码实现无法说明对应哪项需求。

#### 14.11.15 设计文档生成流程

需求确认后，必须按照以下顺序执行：

1. 重新读取当前对话中用户最后确认的需求。
2. 读取根目录的 `AGENTS.md`。
3. 读取 `docs/PROJECT_STATUS.md`。
4. 读取已有的相关问题记录、技术决策和架构文档。
5. 检查项目现有前端、后端、数据库、智能体和测试结构。
6. 查找是否已经存在相同或相似功能。
7. 创建或更新 `docs/features/<feature-name>/`。
8. 先完成 `REQUIREMENTS.md`，并逐条核对用户最后确认的需求。
9. 根据需求完成前端、后端、接口和数据设计。
10. 完成验收标准和需求追踪关系。
11. 执行需求完整性、设计一致性和范围控制检查。
12. 更新 `docs/README.md` 中的功能文档索引。
13. 根据实际影响更新项目总文档。
14. 向用户说明设计文档位置、主要设计结论和仍待确认事项。
15. 再开始功能编码。

除非用户明确要求“只讨论方案，不修改文件”，否则在具备项目文件写入能力时，应实际创建或更新文档，而不是仅在对话中输出文档内容。

#### 14.11.16 需求一致性检查

设计文档完成后，必须逐项执行以下检查。

##### 需求完整性

* 用户明确要求的功能是否全部记录。
* 用户明确排除的功能是否被标记为不包含。
* 是否遗漏角色、权限、状态、数据或异常流程。
* 是否存在未解决但会阻塞开发的问题。
* 每个需求是否具有可验证的验收条件。

##### 设计一致性

* 前端设计是否对应最终需求。
* 后端业务规则是否对应最终需求。
* 接口字段是否与前后端设计一致。
* 数据结构是否能够支持所有业务规则。
* 权限规则在需求、前端、后端和接口文档中是否一致。
* 错误处理是否覆盖主要失败场景。
* 验收标准是否覆盖全部功能需求。
* 文件规划是否符合项目现有目录和依赖方向。

##### 范围控制

* 是否擅自增加用户没有要求的功能。
* 是否因为个人偏好改变用户确认的交互方式。
* 是否为了技术实现方便而改变业务规则。
* 是否加入没有明确价值的扩展设计。
* 是否遗漏用户明确提出的限制条件。
* 是否把规划中的功能写成当前必须实现的功能。

发现不一致时，必须先修正文档，不得带着已知偏差开始编码。

#### 14.11.17 需求优先级和冲突处理

功能实现时，信息优先级如下：

1. 用户最后一次明确确认的需求。
2. 当前功能目录中的 `REQUIREMENTS.md`。
3. 用户确认过的需求变更记录。
4. `API_DESIGN.md`、`FRONTEND_DESIGN.md`、`BACKEND_DESIGN.md` 和 `DATA_DESIGN.md`。
5. `docs/DECISIONS.md` 中仍然有效的技术决策。
6. 项目已有代码和历史实现。
7. 智能体自己的建议和默认判断。

如果设计文档与用户最后确认的需求冲突，以用户最后确认的需求为准，并立即同步修正文档。

如果新需求与已有架构或技术决策冲突，不得擅自忽略新需求，也不得直接破坏原架构。应明确说明冲突、影响和可选方案，在获得用户明确决定后再继续相关实现。

如果用户后续明确修改需求，应将新需求视为正式变更，更新 `REQUIREMENTS.md` 和 `CHANGELOG.md`，并重新检查前端、后端、接口、数据和测试设计。

#### 14.11.18 编码阶段门禁

正式编码前必须确认：

* 功能设计目录已经创建或更新。
* `REQUIREMENTS.md` 状态为“已确认”。
* 前端页面、组件和交互职责已经明确。
* 后端模块、业务规则和依赖方向已经明确。
* API 请求、响应、错误码和权限要求已经明确。
* 数据结构、索引、缓存和迁移方案已经明确，或明确说明不涉及。
* 异常流程和降级策略已经明确。
* 验收标准已经明确。
* 不存在阻塞开发的待确认问题。
* 设计已通过需求一致性检查。

在以上条件未满足时，可以进行代码阅读、技术调研、现状分析和原型验证，但不得将未确认假设固化为正式实现。

编码过程中：

* 每项实现必须能够追溯到需求编号。
* 不得实现设计文档之外的新功能。
* 不得为了编码方便擅自修改业务规则。
* 不得用临时实现替代已确认的完整功能而不说明。
* 发现设计无法实现或存在明显风险时，应暂停相关部分，更新问题记录并说明原因。
* 小范围内部实现调整可以直接记录到设计变更中。
* 会改变用户操作、接口契约、数据结构、权限规则或业务结果的调整，必须重新获得用户确认。

#### 14.11.19 功能实现后的文档同步

功能实现后必须根据实际结果更新：

```text
docs/features/<feature-name>/README.md
docs/features/<feature-name>/ACCEPTANCE_CRITERIA.md
docs/features/<feature-name>/CHANGELOG.md
docs/PROJECT_STATUS.md
docs/DEVELOPMENT_LOG.md
```

根据实际变化决定是否更新：

```text
docs/ARCHITECTURE.md
docs/DECISIONS.md
docs/QUESTIONS.md
docs/README.md
```

只能根据实际验证结果更新状态：

* 代码已写但未检查：`待验证`
* 部分实现完成：`进行中`
* 检查通过但尚未联调：准确说明已通过的范围
* 前后端联调完成且达到验收标准：`已完成`
* 存在阻塞问题：`已阻塞`

不得因为文档已经设计完成，就把功能实现状态标记为“已完成”。

#### 14.11.20 文件操作真实性

只有实际在项目中创建或修改文件后，才可以回复：

```text
已创建功能设计文档。
```

如果当前环境没有项目文件写入能力，必须明确说明：

```text
当前无法直接写入项目目录，以下内容是建议写入的文档草案。
```

不得声称已经创建、更新、同步或验证实际不存在的文件。

#### 14.11.21 自动触发规则

在需求讨论中，检测到用户明确确认重大功能需求后，应自动执行以下动作，无需用户再次提醒：

1. 判断当前功能是否符合 14.11.1 的适用范围。
2. 检查是否存在阻塞实现的待确认问题。
3. 读取现有项目结构、代码和相关文档。
4. 创建或更新 `docs/features/<feature-name>/`。
5. 生成与最终需求一致的详细前端、后端、接口、数据和验收设计文档。
6. 更新 `docs/README.md` 的文档索引。
7. 根据实际影响更新 `PROJECT_STATUS.md`、`QUESTIONS.md`、`DECISIONS.md` 或 `ARCHITECTURE.md`。
8. 完成需求一致性检查。
9. 在回复中列出创建或修改的文档，以及仍存在的风险或待确认事项。

自动触发不代表可以绕过需求确认。只有用户已经明确确认，并且不存在阻塞性问题时，才能生成正式设计文档。

设计文档生成完成后，不应自动开始大范围编码，除非用户已经明确要求“按确认需求实现”“开始开发”或表达了同等含义的指令。

## 15. 最终回复中的文档同步说明

每次完成代码任务后，最终回复除说明代码修改外，还需要说明：

1. 更新了哪些项目文档。
2. 是否创建或更新了 `docs/features/<feature-name>/` 功能设计目录。
3. 项目进度发生了什么变化。
4. 是否新增了问题记录。
5. 是否新增或修改了技术决策。
6. 需求、前端、后端、接口、数据和验收设计是否保持一致。
7. 是否存在未同步、未验证或无法确认的信息。

如果本次任务不需要更新文档，也应明确说明：

> 本次修改未改变项目功能状态、架构或技术决策，因此未更新项目文档。

不得在需要同步文档时仅在回复中说明，而不实际修改 `docs/` 目录中的文件。
