# 项目开发约束

## 1. 项目定位

本项目是一个需要持续迭代和维护的正式工程，不是一次性演示项目。

“MVP”只代表当前功能范围有限，不代表可以降低以下方面的质量要求：

* 项目结构
* 模块边界
* 类型安全
* 错误处理
* 可测试性
* 可维护性
* 数据库设计
* 接口设计
* 安全性
* 后续扩展能力

不要使用“对于 MVP 项目已经足够”“先这样实现就行”等理由，来合理化明显不合理的目录结构、职责混乱、超大文件、重复代码、硬编码或临时方案。

在避免过度设计的同时，必须保持合理的工程结构。优先选择简单、清晰、可扩展的实现，而不是最快但难以维护的实现。

## 2. 开发前必须先分析

在新增功能或进行较大修改之前，先检查现有项目结构和相关代码，确认：

1. 功能应当属于哪个模块。
2. 当前项目是否已有相似实现可以复用。
3. 修改会影响哪些前端页面、后端接口、数据库表或智能体工作流。
4. 是否会造成重复逻辑、循环依赖或职责混乱。
5. 是否需要补充类型、测试、错误处理或文档。
6. 新代码应放在哪个已有目录中，是否真的需要创建新目录。

不得在不了解现有结构的情况下直接创建大量新文件或重新实现已有功能。

对于非简单修改，在开始编码前先简要说明：

* 对需求的理解
* 涉及的模块
* 计划修改的文件
* 采用该方案的原因
* 可能存在的风险

## 3. 架构与目录结构

始终优先保证职责清晰和依赖方向合理。

### 前端职责

前端代码建议按照以下职责划分：

* `pages` 或 `routes`：页面级组合、路由入口
* `components`：可复用 UI 组件
* `features`：按照业务功能组织的模块
* `hooks`：可复用状态逻辑和副作用逻辑
* `services` 或 `api`：HTTP、WebSocket 和其他外部请求
* `stores`：跨组件共享状态
* `types`：共享 TypeScript 类型
* `utils`：无业务状态的纯工具函数
* `constants`：明确的共享常量

遵守以下规则：

* 不要在展示型组件中直接编写复杂接口请求。
* 不要把大量业务逻辑堆积在页面组件中。
* 不要把所有共享逻辑都放入一个通用 `utils` 文件。
* 页面负责组合，业务模块负责业务，组件负责展示。
* API 请求、错误转换和认证处理应尽量集中管理。
* React 组件应保持单一职责，复杂状态逻辑优先抽取到 Hook 或业务模块。
* 不要为了减少文件数量，把无关职责合并在同一个文件中。

### 后端职责

后端代码应按照职责进行分层：

* `routers`：路由声明、参数接收、依赖注入和响应返回
* `schemas`：请求与响应的数据结构
* `services`：业务规则和用例编排
* `repositories`：数据库访问和持久化逻辑
* `models`：ORM 数据模型
* `agents`：智能体定义、工具注册和执行逻辑
* `workflows`：LangGraph 或其他工作流编排
* `retrieval`：文档切分、向量检索、召回和重排
* `core`：配置、安全、日志和公共基础能力
* `integrations`：第三方模型、存储和外部服务适配

遵守以下规则：

* 路由层不得堆积复杂业务逻辑。
* Service 层不得直接依赖 HTTP 请求或响应对象。
* 数据库查询不得散落在路由、智能体节点和工具函数中。
* 智能体工作流与普通业务服务应保持清晰边界。
* Prompt、模型配置和业务代码不得全部混合在一个文件中。
* 不要让单个“万能服务”负责认证、检索、模型调用、数据库和响应格式化。
* 模块依赖应尽量保持单向，避免循环依赖。

## 4. 文件行数限制

所有人工编写的源代码文件，原则上不得超过 350 行。

具体要求：

* 推荐单个文件保持在 100～250 行。
* 文件接近 300 行时，应检查是否承担了过多职责。
* 文件超过 350 行时，必须在完成任务前进行合理拆分。
* 不得通过删除空行、压缩代码或把多条语句写在同一行的方式规避限制。
* 拆分必须基于职责、领域或功能边界，不能为了行数而机械拆分。
* 拆分后不得造成循环依赖、过多转发文件或难以理解的调用链。

以下文件可以例外：

* 自动生成的代码
* 数据库迁移文件
* 依赖锁文件
* 大型静态数据文件
* 由框架或工具自动维护的文件

如果由于特殊原因必须让人工维护的文件超过 350 行，需要明确说明：

* 为什么无法合理拆分
* 文件当前包含哪些职责
* 后续可以如何重构

## 5. 函数和组件约束

* 函数应只完成一个明确任务。
* 普通函数尽量控制在 50 行以内。
* 复杂函数应拆分为有明确名称的子函数。
* 避免超过三层的深度嵌套。
* 避免包含大量布尔参数的函数。
* 避免过长的参数列表，必要时使用明确的数据对象。
* React 页面或组件过于复杂时，应拆分展示组件、业务 Hook 和数据访问逻辑。
* 不要创建只有一行转发逻辑、没有实际抽象价值的文件或函数。

## 6. 代码质量

编写代码时必须遵守以下原则：

* 优先复用现有能力，不重复实现相同逻辑。
* 使用清晰、准确的命名，不使用含义模糊的缩写。
* 避免魔法数字、魔法字符串和散落的配置值。
* 不使用临时占位实现冒充完整功能。
* 不遗留无说明的 `TODO`、`FIXME` 或被注释掉的旧代码。
* 注释应解释“为什么这样做”，而不是重复描述代码本身。
* 不捕获异常后静默忽略。
* 错误信息应对开发者可定位，对用户可理解。
* 外部输入必须进行校验。
* 对可能为空、失败或超时的操作进行明确处理。
* TypeScript 中避免随意使用 `any`、非空断言和不安全的类型转换。
* Python 代码应尽量提供完整类型注解。
* 不得在代码中硬编码密钥、Token、密码或生产环境地址。

## 7. 数据库与接口设计

* 数据表、字段和关系必须反映真实业务含义。
* 不要为了快速实现，把结构化数据全部存入一个 JSON 字段。
* 明确主键、唯一约束、外键、索引和删除策略。
* 修改数据库结构时，应考虑已有数据和迁移方案。
* 避免在循环中进行大量数据库查询。
* 接口应保持稳定、统一和可预测。
* 请求和响应结构必须有明确类型。
* 错误响应应采用统一格式。
* 分页、筛选、排序和权限校验应由后端明确实现。
* 不要直接把数据库模型作为所有接口的响应结构。

## 8. 知识库与智能体约束

涉及 RAG、LangChain、LangGraph 或智能体功能时，需要明确区分：

* 文档加载
* 文档解析
* 文本切分
* Embedding
* 向量存储
* 检索
* 重排
* 上下文构造
* Prompt 管理
* 模型调用
* Tool Calling
* 工作流状态
* 最终回答生成

遵守以下规则：

* 不要把完整 RAG 流程写在单个路由或单个服务文件中。
* Prompt 应集中管理，并具有明确名称和用途。
* 模型供应商相关代码应通过适配层隔离。
* 工作流节点只处理自身职责，不承担整个业务流程。
* 工作流状态应有明确的数据结构和类型。
* 工具调用必须校验参数并处理失败情况。
* 检索结果、模型回答和工具结果应能被区分和追踪。
* 不要依赖无法解释的隐式全局状态。
* 对超时、空检索结果、模型失败和格式异常提供降级处理。
* 调整检索策略时，应说明召回率、准确率、延迟和成本之间的取舍。

## 9. 依赖管理

* 优先使用项目已有依赖和标准库。
* 不要为了一个简单功能引入大型依赖。
* 新增生产依赖前，先说明：

  * 现有方案为什么不能满足需求
  * 新依赖解决什么问题
  * 是否存在更轻量的替代方案
  * 对构建体积、安全性和维护成本的影响
* 不得未经说明升级主要框架或进行大版本升级。
* 前端依赖统一使用项目当前指定的包管理器。
* 不要混用 npm、pnpm 和 yarn。

## 10. 修改范围控制

* 修改应聚焦当前任务。
* 不要顺手重构大量无关代码。
* 不要随意更改已有公开接口、数据结构或配置格式。
* 必须进行破坏性修改时，要明确说明影响范围和迁移方法。
* 优先延续项目已有命名、目录和代码风格。
* 如果现有结构明显不合理，可以提出重构方案，但不要在没有说明的情况下大范围改写。
* 不要为了“代码更少”牺牲可读性和模块边界。

## 11. 测试与检查

修改完成后，根据项目现有能力执行适用的检查：

* 格式化检查
* Lint
* TypeScript 类型检查
* Python 类型检查
* 单元测试
* 接口测试
* 构建检查

新增或修改以下逻辑时，应优先补充测试：

* 登录认证
* Token 刷新
* 权限判断
* 数据转换
* 数据库操作
* 检索逻辑
* 工作流状态转换
* 错误处理
* 核心业务规则

如果无法执行检查，必须明确说明：

* 哪些检查没有执行
* 为什么无法执行
* 可能存在什么风险

不得在未执行测试时声称代码已经完全验证。

## 12. 方案判断原则

进行技术方案选择时，按照以下优先级判断：

1. 正确性
2. 安全性
3. 可维护性
4. 与现有架构的一致性
5. 可测试性
6. 实现复杂度
7. 开发速度

MVP 可以减少功能范围，但不能成为牺牲正确性、安全性和基本工程质量的理由。

避免以下表达和处理方式：

* “对于 MVP 来说已经足够。”
* “先全部写在一个文件里，后面再拆。”
* “暂时硬编码，之后再处理。”
* “现在不需要考虑错误情况。”
* “项目不大，所以不需要分层。”
* “先复制一份相似代码，之后再统一。”

应改为说明具体取舍：

* 当前方案解决什么问题。
* 为什么适合当前项目规模。
* 保留了哪些扩展边界。
* 暂时没有实现哪些功能。
* 未实现部分是否会形成技术债务。
* 后续需求增长时应从哪里扩展。

## 13. 完成任务后的输出

完成修改后，使用简体中文简要说明：

1. 修改了哪些文件。
2. 每个文件承担什么职责。
3. 为什么采用当前结构。
4. 是否新增或修改依赖。
5. 是否涉及数据库或接口变化。
6. 执行了哪些检查或测试。
7. 是否存在尚未解决的风险。

不要只回复“已完成”或“这个实现对于 MVP 已经足够”。

## 14. 项目文档与开发过程同步

本项目需要持续维护项目文档。文档更新属于开发任务的一部分，不是可选步骤。

当代码、项目结构、接口、数据库、功能状态、技术方案或开发计划发生变化时，必须同步检查并更新 `docs/` 目录中的相关文档。

不要只在对话中说明项目变化，而不更新项目内的文档。

### 14.1 文档目录

项目文档统一存放在：

```text
docs/
├── README.md
├── PROJECT_STATUS.md
├── DEVELOPMENT_LOG.md
├── QUESTIONS.md
├── DECISIONS.md
└── ARCHITECTURE.md
```

各文件职责如下：

* `docs/README.md`：文档索引，说明各文档的用途。
* `docs/PROJECT_STATUS.md`：当前项目整体进度和下一步计划。
* `docs/DEVELOPMENT_LOG.md`：按照时间记录实际开发过程。
* `docs/QUESTIONS.md`：记录开发过程中提出的重要问题及最终结论。
* `docs/DECISIONS.md`：记录重要技术决策、选择理由和影响。
* `docs/ARCHITECTURE.md`：记录当前项目结构、模块职责和主要数据流。

如果这些文件不存在，可以在首次需要时创建。

不要为每次开发任务创建新的零散文档。优先维护以上固定文件，避免 `docs/` 目录出现大量职责重复、命名混乱的 Markdown 文件。

### 14.2 开始任务前

开始处理项目任务前，先读取：

1. 根目录的 `AGENTS.md`。
2. `docs/PROJECT_STATUS.md`。
3. 与当前任务相关的架构、决策和开发记录。
4. 当前任务涉及的源代码和测试。

开始编码前，需要确认：

* 当前项目处于什么阶段。
* 当前正在开发什么功能。
* 是否存在相关的历史决策。
* 当前任务是否与已有计划冲突。
* 是否存在未完成事项、阻塞项或已知风险。
* 是否已经有相似功能，避免重复实现。

不得只根据当前对话内容判断项目状态，而忽略项目中已有的文档和代码。

如果项目文档与实际代码不一致，以实际代码和可验证的运行结果为依据，并在本次任务中修正文档。

### 14.3 项目进度同步

`docs/PROJECT_STATUS.md` 是项目当前状态的唯一主要入口。

该文件至少维护以下内容：

```md
# 项目当前状态

## 当前阶段

说明项目目前处于需求设计、基础架构、功能开发、联调、测试或部署中的哪个阶段。

## 当前目标

说明当前阶段最重要的目标。

## 已完成功能

- 已完成并经过验证的功能
- 对应的模块
- 验证状态

## 正在进行

- 当前正在开发的功能
- 已完成部分
- 尚未完成部分

## 下一步计划

1. 下一项具体任务
2. 后续任务
3. 需要准备的内容

## 阻塞问题

- 当前阻塞项
- 阻塞原因
- 解除阻塞所需条件

## 已知风险与技术债务

- 风险或技术债务
- 可能造成的影响
- 建议处理时间

## 最近更新时间

YYYY-MM-DD
```

发生以下情况时，必须更新 `PROJECT_STATUS.md`：

* 完成一个功能或阶段性目标。
* 开始一个新的主要功能。
* 项目计划发生变化。
* 新增或解除阻塞问题。
* 发现重要风险或技术债务。
* 修改主要项目结构。
* 修改核心接口、数据库或智能体工作流。
* 原有进度记录与实际代码不一致。

只记录真实且经过确认的状态。

不得因为生成了代码就直接将功能标记为“已完成”。只有完成必要检查，并确认功能达到当前验收条件后，才可以标记为已完成。

状态建议使用：

* `未开始`
* `进行中`
* `已完成`
* `已阻塞`
* `待验证`

不要使用含义不明确的状态，例如：

* `差不多完成`
* `基本可用`
* `MVP 已经够用`
* `后面再优化`

### 14.4 开发过程记录

每次完成有实际意义的代码修改后，在 `docs/DEVELOPMENT_LOG.md` 中追加开发记录。

使用以下格式：

```md
## YYYY-MM-DD：任务名称

### 任务目标

说明本次任务准备解决什么问题。

### 实现内容

- 实现了哪些功能
- 修改了哪些模块
- 增加了哪些关键逻辑

### 主要文件

- `path/to/file`：说明该文件的修改内容和职责
- `path/to/file`：说明该文件的修改内容和职责

### 技术方案

说明采用了什么方案，以及选择该方案的主要原因。

### 接口或数据变化

- 是否新增或修改接口
- 是否修改请求和响应结构
- 是否修改数据库表或字段
- 是否需要执行迁移

没有变化时明确写“无”。

### 验证情况

- 执行了哪些测试
- 执行了哪些检查
- 哪些检查通过
- 哪些检查未执行及原因

### 遗留问题

- 尚未完成的内容
- 已知风险
- 后续建议
```

开发日志按时间顺序维护。

不要修改或删除过去的有效记录来掩盖问题。如果之前的记录已经失效，应追加更正说明，而不是直接让历史记录消失。

以下情况可以不单独记录：

* 只修改错别字。
* 只调整无业务影响的格式。
* 没有改变代码行为的小范围注释修正。
* 没有实际修改项目文件的咨询性对话。

### 14.5 用户问题记录

对于用户提出的、与当前项目开发直接相关且具有长期参考价值的问题，应记录到：

```text
docs/QUESTIONS.md
```

需要记录的问题包括：

* 项目结构应该如何设计。
* 某个功能应该放在哪一层。
* 为什么选择某项技术。
* 接口、数据库或认证应该如何设计。
* 某个错误的原因和解决方式。
* 项目需求或业务规则的澄清。
* 对后续开发有影响的约束。
* 用户最终确认的实现方向。

以下内容通常不需要记录：

* 单纯询问某行代码的语法。
* 与项目无关的通用知识问题。
* 重复提出且结论没有变化的问题。
* 临时交流和无长期价值的内容。
* 尚未形成结论的随意猜测。

问题记录使用以下格式：

```md
## Q-YYYYMMDD-序号：问题标题

- **日期**：YYYY-MM-DD
- **状态**：待确认 / 已确认 / 已解决
- **涉及模块**：模块名称
- **用户问题**：准确概括用户提出的问题
- **问题背景**：说明为什么会提出这个问题
- **最终结论**：记录经过确认的答案或决定
- **实现影响**：说明会影响哪些模块、接口或文件
- **后续事项**：仍需处理的内容
```

记录用户问题时，需要进行有意义的概括，不要机械复制整段对话。

重点记录：

* 问题本身；
* 形成的结论；
* 对项目产生的影响；
* 是否还有后续工作。

同一问题被多次讨论时，应优先更新已有记录，不要重复创建多个内容相同的问题条目。

如果问题尚未形成可靠结论，应标记为“待确认”，不得把推测写成最终决定。

### 14.6 技术决策记录

发生重要技术选择时，应更新：

```text
docs/DECISIONS.md
```

需要记录的决策包括：

* 选择某个框架、数据库或核心依赖。
* 调整前后端目录结构。
* 修改认证方式。
* 修改接口规范。
* 修改数据库关系。
* 修改 RAG 检索流程。
* 修改智能体工作流。
* 引入新的第三方服务。
* 进行可能影响后续扩展的架构选择。

使用以下格式：

```md
## ADR-序号：决策标题

- **日期**：YYYY-MM-DD
- **状态**：提议 / 已采用 / 已废弃 / 已替代

### 背景

说明需要解决的问题和约束。

### 可选方案

1. 方案一
2. 方案二
3. 方案三

### 最终决定

说明采用哪个方案。

### 选择理由

说明为什么采用该方案，而不是其他方案。

### 影响与代价

说明方案带来的优点、限制、风险和维护成本。

### 后续条件

说明在什么情况下需要重新评估该决策。
```

不要把普通代码实现细节全部写成技术决策。只有对项目结构、长期维护或后续扩展有明显影响的选择，才需要进入决策记录。

### 14.7 架构文档同步

`docs/ARCHITECTURE.md` 应反映项目当前真实结构。

当发生以下变化时，应同步更新：

* 新增主要业务模块。
* 调整目录职责。
* 修改模块依赖方向。
* 增加第三方系统。
* 修改数据库、缓存、向量库或文件存储方案。
* 修改请求链路。
* 修改认证流程。
* 修改 RAG 或智能体执行流程。
* 修改前后端通信方式。

架构文档至少应包含：

* 项目总体说明。
* 前端模块划分。
* 后端模块划分。
* 数据库和存储说明。
* 认证流程。
* 核心接口调用流程。
* 知识库数据处理流程。
* 智能体工作流。
* 外部服务依赖。
* 关键模块之间的依赖关系。

架构文档应描述当前已经存在的实现。

计划中的架构必须明确标记为“规划中”，不能与已经实现的结构混在一起。

### 14.8 每次任务结束前的文档检查

完成任务前，必须执行一次文档同步检查：

1. 本次修改是否改变了功能状态。
2. 是否需要更新 `PROJECT_STATUS.md`。
3. 是否需要追加 `DEVELOPMENT_LOG.md`。
4. 用户是否提出了值得长期记录的问题。
5. 是否形成了新的技术决策。
6. 是否改变了项目架构或模块职责。
7. 文档中的路径、接口和状态是否仍然准确。
8. 文档是否包含未经验证的完成状态。

只更新与本次任务直接相关的文档。

不要为了满足“更新文档”而机械修改所有文档，也不要重写与本次任务无关的历史内容。

文档修改应与代码修改处于同一次任务中，不要把文档同步推迟到不确定的未来。

### 14.9 信息安全要求

项目文档中不得记录：

* 密码
* Access Token
* Refresh Token
* API Key
* 数据库完整连接字符串
* Cookie
* 私钥
* 用户隐私数据
* 生产环境敏感配置
* `.env` 文件中的真实值

需要说明配置时，只记录环境变量名称和用途，例如：

```text
DATABASE_URL：数据库连接地址
JWT_SECRET：JWT 签名密钥
OPENAI_API_KEY：模型服务访问密钥
```

不得记录这些变量的真实值。

### 14.10 文档真实性

所有开发记录必须基于实际修改和验证结果。

遵守以下要求：

* 没有执行测试时，不得写“测试已通过”。
* 没有完成的功能，不得记录为“已完成”。
* 仅创建接口骨架时，应标记为“进行中”。
* 仅完成前端页面时，不得声称整个业务功能已经完成。
* 发现代码与文档不一致时，应明确更正文档。
* 无法确认的信息应标记为“待确认”。
* 推测和规划不得写成已经实现的事实。

## 15. 最终回复中的文档同步说明

每次完成代码任务后，最终回复除说明代码修改外，还需要说明：

1. 更新了哪些项目文档。
2. 项目进度发生了什么变化。
3. 是否新增了问题记录。
4. 是否新增或修改了技术决策。
5. 是否存在未同步或无法确认的信息。

如果本次任务不需要更新文档，也应明确说明：

> 本次修改未改变项目功能状态、架构或技术决策，因此未更新项目文档。

不得在需要同步文档时仅在回复中说明，而不实际修改 `docs/` 目录中的文件。
