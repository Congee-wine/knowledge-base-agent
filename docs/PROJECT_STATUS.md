# 项目当前状态

> 2026-07-27 更新：正式聊天消息列表已改为标准顶部排列，避免新会话首条消息被内置倒序滚动锚定在输入框上方。流式自动跟随改由页面在用户停留底部时控制；用户上滑后保留“回到底部”按钮。自动化测试与生产构建已通过，待浏览器确认首条消息、长回复和流式增量的实际滚动体验。

> 2026-07-27 更新：用户已完成阶段 3 页面层手工测试。阶段 3（前端 SSE 解析与双模式状态机）现标记为“已完成”；下一步进入阶段 4，进行正式聊天与编辑预览的完整页面联调和隔离验收。

> 2026-07-27 更新：正式聊天的消息区继续由 Ant Design X `Bubble.List` 展示；不再使用其倒序 `autoScroll`，以保证短消息从顶部开始。流式回复在用户停留底部时自动跟随，用户上滑查看历史后可通过悬浮图标平滑回到底部。待浏览器端确认长回复和流式增量下的滚动体验。
> 2026-07-27 更新：助手消息调整为透明无底色的左侧内容区，用户消息保留右侧灰色气泡；每条非空助手回复提供复制操作。待浏览器端确认长 Markdown 与流式消息下的视觉和复制体验。

> 2026-07-27 更新：生成中的停止操作改为使用 Ant Design X `Sender` 内置的中断控件；移除了重复的文字按钮，内置控件继续调用相同的正式会话中断或预览本地取消逻辑。

> 2026-07-27 更新：阶段 3 已完成前端实现、自动化验证和用户页面层手工测试。SSE 解析支持分块、CRLF、多行 `data:` 与末尾无空行；停止正式生成会持久化部分回答，停止预览只取消本地请求。下一步为阶段 4：正式聊天和编辑预览页面联调。

> 2026-07-27 更新：阶段 2B 已完成首轮后端实现与服务层验证，状态为“进行中、待前端接入”。正式流式聊天现加载有限有效历史；预览保持零持久化；续流请求明确返回 `STREAM_RESUME_UNAVAILABLE`；受认证的中断接口会保存已生成片段并将助手消息标记为 `interrupted`。下一步为阶段 3：前端解析器、状态机与停止生成接入。

> 2026-07-27 更新：阶段 2A 的双模式 SSE 契约已完成代码实现和自动化验证，状态为“进行中、待浏览器端联调”。预览与正式会话的所有事件现均含 `mode`；预览不要求持久化 ID，正式会话仍要求真实会话/消息 ID。下一项工作是阶段 2B：补齐后端运行生命周期和数据隔离验收。

> 2026-07-27 更新：根据已确认的预览/正式聊天双运行上下文原则，未完成流式工作已重排为阶段 2A（双模式 SSE 契约）、2B（后端生命周期与数据隔离）、3（前端 SSE 状态机）、4（页面联调）和 5（回归验收）。阶段 0/1 保持已完成；现有编辑预览 SSE 与前端正式会话解析契约不一致，阶段 2A 未完成前不得将预览链路标记为已验证。

> 2026-07-27 更新：阶段 1 已补齐会话内消息稳定排序。迁移 `20260727_0008` 已应用到本地 PostgreSQL；15 项仓储测试、4 项服务测试、9 项智能体/会话集成测试和 5 项认证集成测试均通过。完整串行执行超过 120 秒但分模块执行全部通过。

> 2026-07-27 更新：阶段 1 已完成验收。并发幂等请求现在会回滚到保存点并返回已创建的消息对，既有草稿会话首次流式发送会被激活；会话内消息改由稳定序号排序。相关后端测试分模块共 33 项通过。

> 2026-07-27 更新：阶段 2 前端流式消息重复渲染的代码修复和自动化验证已完成，仍待浏览器验收。`message_start` 使用真实 ID 替换、按 ID 去重合并、`message_end` 后待确认消息对与确认清理流程均已实现；每次发送前清空上一请求的真实消息 ID，避免新请求在 `message_start` 前失败时误标记上一条助手消息；SSE 事件在消费前进行运行时结构校验。`mergeMessages` 9 项测试、`useStreamingChat` 7 项测试、SSE 协议校验 2 项测试及既有环境测试共 19 项通过。

## 2026-07-27 当前阶段补充

- **当前目标**：按已确认的阶段 0–5 修复并验证流式智能体运行。
- **已完成**：阶段 0（测试基础与协议边界）；阶段 1（仓储层事务、回复关系和并发幂等性）。
- **正在进行**：阶段 2 的消息 ID 对齐、去重合并、确认清理与 SSE 事件校验已完成自动化验证，待浏览器验收；停止生成与断线续流尚未实现。
- **下一阶段**：完成阶段 2 的浏览器验收后，实现停止生成、Redis 事件缓冲与断线续流。
- **未完成范围**：前端 SSE 解析、停止生成、Redis 续流、知识库、联网、文件上传、引用与受控处理摘要。

## 当前阶段

流式智能体运行修复阶段（阶段 A）。阶段 0 已完成测试基础搭建；阶段 1 已完成消息仓储事务、回复关系、状态更新与并发幂等性修复。SSE 生命周期、前端状态机与正式/预览联调仍待修复与验证。知识库、联网、文件解析和 Redis 断线续流不在当前实现范围。

## 当前目标

进入阶段 2：修复 SSE 生命周期、停止生成与前端状态机，完成后进行浏览器人工联调。

认证前端路由现已按匿名区、受保护应用区和 404 页面划分；后续新增 AI、智能体和知识库页面应继续挂载在 `/app/*` 下。

## 已完成功能

- 认证 API：`POST /api/auth/register`、`/login`、`/refresh`、`/logout` 和 `GET /api/auth/me` 已在 `backend/routers/auth.py` 定义；请求与响应模型位于 `backend/schemas/auth.py`。
- 认证持久化：`users`、`auth_sessions`、`revoked_tokens` 表和 `vector` 扩展由 Alembic 基线迁移 `20260720_0001` 管理；已与本地 PostgreSQL 现有表对齐并完成集成验证。
- 密码与令牌：服务层使用 Argon2 密码哈希、JWT 访问令牌和可轮换的刷新令牌；实现位于 `backend/services/auth.py`。
- 前端认证界面与状态：注册/登录页面、认证 API 封装、浏览器存储会话、刷新令牌逻辑和基于用户状态的路由已存在于 `frontend/src/`。
- 前端路由边界：`/login`、`/register`、`/app/*` 与 404 页已拆分；受保护页面会在登录后回跳至原始访问地址。已通过 `pnpm build`（2026-07-18）。
- 受保护接口骨架：`POST /api/chat` 需要 Bearer 认证，但当前仅返回回显文本，不是 AI 或 RAG 回答。
- 已验证的构建检查（2026-07-18）：后端关键 Python 模块的 `py_compile` 通过；前端 `pnpm build` 通过。
- PostgreSQL 认证迁移与集成验证（2026-07-22）：确认现有 `users`、`auth_sessions`、`revoked_tokens` 表与认证基线迁移一致后，已执行 Alembic `stamp head`；注册、登录、`/me`、刷新令牌、登出、撤销令牌校验和受保护聊天回显接口均通过真实 PostgreSQL 验证。
- 认证自动化测试（2026-07-22）：新增 5 个 `unittest` 集成测试，覆盖重复注册、错误密码、刷新令牌轮换与重放拒绝、登出后令牌撤销和过期会话拒绝；均已通过，且测试数据已清理。
- 本地异步基础设施（2026-07-22）：Docker Compose 启动 Redis、MinIO 与 Linux 标准 RQ Worker；验证 Redis `ping`、私有 Bucket 创建、测试文本写读删除及由 Linux Worker 执行的 RQ 测试任务均通过。
- 智能体与会话后端基础（2026-07-23）：Alembic `20260723_0002` 创建智能体、用户默认设置、预设问题、会话和消息表，并写入内置 AI 管家种子记录；智能体 CRUD、默认设置/清空、聊天入口、会话查询和消息回显 API 通过真实 PostgreSQL 集成测试。
- PostgreSQL 连接池（2026-07-23）：FastAPI 使用共享 `psycopg_pool` 连接池，消除每个受保护请求反复建立 PostgreSQL 连接的等待；认证与会话集成测试共 13 项通过。
- 智能体管理前后端联调（2026-07-24）：已实现真实智能体列表、动态侧栏、新建/编辑表单、默认打开切换、删除确认与指定智能体聊天路由。内置 AI 管家只能使用或恢复为默认打开；个人智能体删除后不再可从列表、侧栏、聊天页或会话列表访问。
- 联网入口配置（2026-07-24）：迁移 `20260724_0004` 为 `agents` 增加 `allow_network_access`；个人智能体创建/编辑后可控制聊天输入区联网按钮的显示，按钮仍只提示“功能暂未支持”。
- 智能体列表视觉重构（2026-07-24）：按用户提供的设计稿重构顶部横幅、分类栏、四列卡片、标签/编辑信息和侧栏导航节奏；原设计中的插画与头像资产尚未提供，当前使用可替换的本地占位图形。
- 认证恢复稳健性（2026-07-24）：热更新、网络中断或浏览器取消刷新请求不再被误判为登录失效；仅在凭证明确被拒绝或缺少刷新令牌时退出登录。

## 正在进行

- 智能体创建流程正在扩展为“基础信息弹窗 → 完整配置页”：已新增文本交互类型、私有头像上传/读取接口和前端弹窗；已执行数据库迁移与静态检查，仍待浏览器端实际上传和跳转流程验收。
- 智能体完整配置页已重构为三栏搭建工作台：当前可保存欢迎语、预设问题、描述、系统提示词及上传/联网入口显示；知识集绑定、真实联网/上传、语音热词与公开访问依赖后续服务，当前不伪装为可用。
- 智能体完整配置页的系统提示词已替换为 `@uiw/react-md-editor` 的固定单栏 Markdown 编辑器，提供加粗、斜体、标题、引用、无序/有序列表、代码、表格、帮助与预览；通过 `preview="edit"` 收起默认预览列，文本框与预览均在中栏同一编辑区域全宽显示，且工具栏不提供双栏模式，类型检查通过，待浏览器人工验收。
- 智能体配置页已修正带字段名的表单项包含多个直接子节点的问题；上传和联网单选已恢复由表单正确绑定，缺失配置值仍默认“需要”。
- 智能体配置展示同步（2026-07-25，待浏览器验收）：已统一编辑页、卡片和导航的头像加载；自建智能体聊天欢迎区与编辑预览复用同一展示组件及输入框主体。AI 管家保留原欢迎样式，预设问题改由数据库读取；`20260725_0005` 迁移已应用到本地数据库并写入 7 条内置问题。
- 预设问题预览同步修复（2026-07-25，待浏览器验收）：`Form.List` 的局部变更不再覆盖完整预览数组，右侧预览应显示当前填写的全部问题。
- 共享智能体运行与实时预览（2026-07-25，设计已确认、待实现）：首期接入 DeepSeek 文本问答，正式聊天持久化、编辑预览不持久化，并共用运行服务；知识库、真实联网和对话文件上传延后至模型运行稳定后的独立阶段。
- 流式协议与处理思路策略（2026-07-25，待确认实施计划）：阶段 A 优先实现带 `requestId`、`sequence`、幂等与断线恢复的流式最终回答；知识库、引用和受控处理思路分别放在后续阶段，不展示完整原始思维链。
- 阶段 2 的自动化验证和智能体列表视觉重构已完成；仍需按真实浏览器流程人工检查创建、编辑、设默认、删除、切换聊天与设计稿像素级差异。
- 真实模型 SSE、前端流式渲染、知识库和文件处理尚未实现。

## 下一步计划

1. 完成智能体管理的浏览器人工联调，并修正发现的交互问题。
2. 建立知识库领域的数据迁移、异步文件处理、资料树与向量索引。
3. 在可控资料范围完成后，接入 DeepSeek、检索、bge-m3 与 SSE 流式回答，并执行完整验收测试。

## 阻塞问题

- 本地后端 `.env` 的 `OBJECT_STORAGE_ACCESS_KEY` 与 `OBJECT_STORAGE_SECRET_KEY` 与 Compose 中正在使用的 MinIO 开发凭据不一致，导致带头像创建请求被拒绝；需同步配置并重启后端。
- 无当前代码级阻塞。Redis 与 MinIO 容器需在本地开发时保持运行；生产环境需以独立的受控服务和非默认凭据替代本地 Compose 配置。

## 已知风险与技术债务

- 后续业务表必须继续通过 Alembic 迁移管理，禁止恢复启动时自动建表；部署环境必须在应用启动前执行迁移并设置强随机 `AUTH_SECRET_KEY`。
- 前端将访问令牌和刷新令牌存入 `localStorage`；XSS 发生时令牌可能暴露。生产场景应重新评估 HttpOnly Cookie 等方案。
- 当前生产构建的未压缩 JavaScript 主包约为 2.13 MB；新增 Markdown 编辑器及其渲染/高亮依赖后包体显著增加。路由结构已具备后续按页面进行懒加载和代码分割的边界，但尚未实施。
- 本地 Compose 中的 MinIO 初始账号仅限开发环境；部署前必须改为受控凭据、私有网络和最小权限访问策略。
- 阶段 2 的会话列表尚未实现 cursor 翻页，当前以最多 100 条的参数限制返回；前端接入和数据量增长前应补齐游标契约。
- 连接池消除了反复建连成本，但同一 PostgreSQL 连接上的简单查询本机实测仍约 70–230ms；若用户操作仍感到缓慢，应继续分析 SQL 查询数量、索引和本机 PostgreSQL/容器网络延迟。
- `frontend/docs/login.md` 中的部分描述与当前实现不一致（例如其提及 SQLite/后续刷新令牌，而当前代码使用 PostgreSQL 且已实现刷新令牌）。该文件不在本次统一 `docs/` 文档范围内，需后续单独校正。

## 2026-07-25 流式聊天阶段 A 更正

- 状态：进行中。
- 已完成：DeepSeek 流式适配、正式会话 SSE 路由、草稿预览 SSE 路由、消息请求幂等字段迁移，前端正式聊天和编辑预览对 SSE 的首次接线，以及前端流式消息重复渲染修复（真实 ID 替换、按 ID 去重、待确认消息对与确认清理）。
- 待完成：浏览器端实测、停止生成、Redis 事件缓冲和断线续流；在这些完成前，阶段 A 不得标记为已完成。

## 2026-07-25 工作流与渲染纠正

- 进行中：阶段 A 模型调用已从直接 HTTP 适配改为 LangChain OpenAI 兼容模型与 LangGraph 最小运行图。
- 已完成：流式聊天恢复三点 loading，助手气泡支持 Markdown 流式渲染与生成状态文案。
- 未完成：受控处理摘要、知识库执行步骤、联网步骤、停止生成和 Redis 续流。

## 最近更新时间

2026-07-27

## 2026-07-23 界面实现更正

- AI 管家欢迎页已按确认原型改为纯白聊天画布、右上角文字操作、左侧问候与纵向预设问题、底部紧凑输入区；不显示“新建一个文档”卡片。
- 当前只展示后端已真实提供的 AI 管家，不在侧栏伪造尚未接入的个人智能体数据。
- 前端已通过 TypeScript 检查和生产构建；新建会话、会话列表刷新和历史会话详情读取已完成前端联调，消息发送与历史消息展示仍是下一步。
- 认证刷新已加入单飞和跨标签页轮换保护；服务端明确拒绝 refresh token 时仍会退出登录。
- 聊天页面的“新会话”改为纯前端空白状态；首次非空发送才由统一消息接口在一个事务中创建会话和消息，因此历史记录不会出现空会话。

## 阶段 1 实施更正（2026-07-20）

- 状态：已完成。
- 已完成：Alembic 认证基线迁移、配置项、对象存储和 RQ 队列适配边界；FastAPI 不再在启动时直接建表。
- 已验证：新增 Python 依赖安装、Python 静态编译和 Alembic 离线 SQL 生成。
- 已验证：Alembic 可连接现有 PostgreSQL，API 健康检查和 RQ 队列对象构造通过。
- 已验证：旧认证表结构与基线一致后已执行 `stamp`；Redis、MinIO 连通、私有 Bucket 写读删除和 RQ 测试任务执行通过。
- 本地运行：通过 `docker-compose.infrastructure.yml` 启动 Redis、MinIO 与 Linux 标准 RQ Worker；验证任务已由容器内的标准 Worker 成功执行。
