# 验收标准

- 所有者可预览四种已处理完成的格式；其他用户不能通过猜测 ID 读取内容。
- TXT 与 Markdown 真实内容可读取，空内容有明确提示。
- PDF 在当前应用内打开，刷新与离开页面不会留下 Blob URL。
- DOCX 标题、段落、列表和表格可显示，`<script>` 等输入文字不会变为可执行 HTML。
- 资料树双击文件和右键“预览”均进入同一页面；文件夹双击不变。
- 处理中、失败、空内容、对象存储失败和不支持格式均显示明确的用户提示。
- 后端单元测试、前端测试与 TypeScript 构建通过，并进行浏览器验收。
