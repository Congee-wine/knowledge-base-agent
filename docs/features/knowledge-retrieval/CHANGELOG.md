# 变更记录

## 2026-07-29

- 用户确认检索质量升级：采用混合候选召回、本地 BGE 重排、文件级上下文限额、最终来源引用一致性与检索追踪；更新功能设计至 2.0，开始实施。

- 用户确认知识库模式采用“检索必经”策略：运行时不再由大模型判断是否检索；存在 ready 索引文件时，除资料目录查询外所有问题固定执行语义检索。无 ready 文件与无相关结果分别返回固定、可区分的知识库说明，不回退为通用模型回答。

- 修复资料树删除遗漏 `embedding_jobs` 清理的问题；删除事务现在在删除文档版本前移除关联 embedding 任务，避免外键冲突导致删除失败。

## 2026-07-28

- 用户确认采用混合切分、独立 embedding Worker、Top 8/最终 5 条、强制资料范围过滤、可跳转短引用和异步版本化索引方案。
